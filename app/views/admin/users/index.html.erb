<header>
  <h1>Users</h1>

  <%= link_to "New User", new_admin_user_path, class: 'new' %>
</header>

<div id="list-group">
  <ul class="list-group">
    <% @users.each do |user| %>
      <li class="list-group-item"><%= link_to user, admin_user_path(user) %></li>
    <% end %>
  </ul>
</div>
